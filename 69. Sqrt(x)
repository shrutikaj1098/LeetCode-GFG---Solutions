class Solution:
    def mySqrt(self, x: int) -> int:
        i=0
        if x==0:
            return 0
        else:
            while i<=x:
                if(i*i<=x):                
                    i+=1
                else:
                    break

            return i-1
        
