class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        if target in nums:
            i=0
            while target !=nums[i]:
                i+=1
            return i
        else:
            i=0
            while i<len(nums) and target >nums[i]:
                i+=1
            return i
